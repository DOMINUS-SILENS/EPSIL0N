---
phase: 05-projection-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php
  - packages/kernel/resources/sql/event_store/001_create_event_store.sql
autonomous: true
requirements:
  - PROJ-01
must_haves:
  truths:
    - "Every event in the event_store has a unique, monotonically increasing global position"
    - "The event store can be queried by global position for polling"
  artifacts:
    - path: "packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php"
      provides: "Global sequence tracking"
      exports: ["getEventsFromPosition"]
    - path: "packages/kernel/resources/sql/event_store/001_create_event_store.sql"
      provides: "Schema update with BIGSERIAL position"
      contains: "global_position BIGSERIAL"
  key_links:
    - from: "PostgreSqlEventStore"
      to: "event_store table"
      via: "INSERT into global_position"
      pattern: "INSERT INTO \"event_store\""
---

<<objectiveobjective>
Implement Global Sequencing in the PostgreSQL Event Store.

Purpose: Projections need a reliable, monotonically increasing sequence number to poll for new events across all streams, regardless of tenant or aggregate.
Output: Updated database schema and EventStore implementation supporting global position tracking.
</objective>

<<executionexecution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<<contextcontext>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php
</context>

<<taskstasks>

<<tasktask type="auto" tdd="true">
  <<namename>Task 1: Update Event Store Schema</name>
  <<filesfiles>packages/kernel/resources/sql/event_store/001_create_event_store.sql</files>
  <<behaviorbehavior>
    - Ensure the `event_store` table includes a `global_position` column.
    - Column must be `BIGSERIAL` (or `BIGINT GENERATED ALWAYS AS IDENTITY`) to ensure monotonicity.
    - Must be indexed for efficient polling.
  </behavior>
  <<actionaction>
    Modify the SQL migration to add `global_position BIGSERIAL PRIMARY KEY` (or add it as a unique index if the primary key is already the event_id). 
    Given the need for polling, a B-Tree index on `global_position` is mandatory.
  </action>
  <<verifyverify>
    <<automatedautomated># Check if SQL contains global_position
    grep "global_position" packages/kernel/resources/sql/event_store/001_create_event_store.sql
    </automated>
  </verify>
  <<donedone>Schema updated to support global sequencing</done>
</task>

<<tasktask type="auto" tdd="true">
  <<namename>Task 2: Implement Global Position Persistence</name>
  <<filesfiles>packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php</files>
  <<behaviorbehavior>
    - When appending events, the database must automatically assign the global position.
    - The `StoredEvent` object should be updated to capture this `global_position` upon retrieval.
  </behavior>
  <<actionaction>
    Update `performAppend` to ensure `global_position` is populated by the DB.
    Update `load` and `loadReverse` to include `global_position` in the result set.
    Update `StoredEvent::fromDatabaseRow` (if applicable) to map the new column.
  </action>
  <<verifyverify>
    <<automatedautomated># Verify the method exists
    grep "getEventsFromPosition" packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php
    </automated>
  </verify>
  <<donedone>EventStore now persists and retrieves global positions</done>
</task>

<<tasktask type="auto" tdd="true">
  <<namename>Task 3: Implement Global Polling API</name>
  <<filesfiles>packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php</files>
  <<behaviorbehavior>
    - Implement `getEventsFromPosition(int $position, int $limit)`
    - Returns a list of `StoredEvent` ordered by `global_position` ASC.
    - Must be tenant-agnostic (global view) as the Projection Engine manages tenant-specific projectors.
  </behavior>
  <<actionaction>
    Add `public function getEventsFromPosition(int $position, int $limit = 100): array` to `PostgreSqlEventStore`.
    The SQL should be: `SELECT * FROM event_store WHERE global_position > :position ORDER BY global_position ASC LIMIT :limit`.
  </action>
  <<verifyverify>
    <<automatedautomated># Run integration test (requires test setup)
    cd packages/kernel && ./vendor/bin/phpunit --filter PostgreSqlEventStoreTest
    </automated>
  </verify>
  <<donedone>Projection engine can now poll events globally</done>
</task>

</tasks>

<<verificationverification>
Integration test: 
1. Append events to 3 different streams/tenants.
2. Call `getEventsFromPosition(0, 10)`.
3. Assert that events are returned in the exact order they were persisted across all streams.
4. Assert that the `global_position` is strictly increasing.
</verification>

<<successsuccess_criteria>
The EventStore provides a stable, monotonically increasing global sequence that can be used as a checkpoint for projections.
</success_criteria>

<<outputoutput>
After completion, create `.planning/phases/05-projection-infrastructure/05-01-SUMMARY.md`
</output>
