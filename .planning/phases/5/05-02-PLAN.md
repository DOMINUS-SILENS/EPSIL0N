---
phase: 05-projection-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - packages/kernel/src/Domain/Shared/Event/IEventProjector.php
  - packages/kernel/src/Domain/Shared/Event/AbstractProjector.php
autonomous: true
requirements:
  - PROJ-02
must_haves:
  truths:
    - "Projectors can handle a single domain event"
    - "Projectors are tied to specific event types they care about"
  artifacts:
    - path: "packages/kernel/src/Domain/Shared/Event/IEventProjector.php"
      provides: "Projection contract"
      exports: ["project"]
    - path: "packages/kernel/src/Domain/Shared/Event/AbstractProjector.php"
      provides: "Base projector logic (filtering/dispatching)"
  key_links:
    - from: "ProjectionEngine"
      to: "IEventProjector"
      via: "method call"
      pattern: "project\(\$event\)"
---

<<objectiveobjective>
Define the Projector Contracts.

Purpose: Establish the interface for transforming event streams into read-model state.
Output: IEventProjector interface and a supporting abstract base class.
</objective>

<<executionexecution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<<contextcontext>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<<taskstasks>

<<tasktask type="auto">
  <<namename>Task 1: Define IEventProjector Interface</name>
  <<filesfiles>packages/kernel/src/Domain/Shared/Event/IEventProjector.php</files>
  <<actionaction>
    Create the `IEventProjector` interface.
    Method: `project(DomainEvent $event): void`
    This is the primary entry point for the ProjectionEngine to push events into a read model.
  </action>
  <<verifyverify>
    <<automatedautomated>ls packages/kernel/src/Domain/Shared/Event/IEventProjector.php</automated>
  </verify>
  <<donedone>Interface defined</done>
</task>

<<tasktask type="auto">
  <<namename>Task 2: Implement AbstractProjector Base Class</name>
  <<filesfiles>packages/kernel/src/Domain/Shared/Event/AbstractProjector.php</files>
  <<actionaction>
    Implement `AbstractProjector` which implements `IEventProjector`.
    - Add a property `protected array $subscribedEvents` to define which event classes this projector handles.
    - Implement `project(DomainEvent $event)` to check if the event's class is in the subscription list before calling a protected `handleEvent()` method.
    - Define `abstract protected function handleEvent(DomainEvent $event): void`.
  </action>
  <<verifyverify>
    <<automatedautomated>ls packages/kernel/src/Domain/Shared/Event/AbstractProjector.php</automated>
  </verify>
  <<donedone>Abstract base class provides filtering logic</done>
</task>

</tasks>

<<verificationverification>
Verify that a concrete implementation of `AbstractProjector` can be created and only processes events listed in its `$subscribedEvents`.
</verification>

<<successsuccess_criteria>
A standard contract exists for all future read-model projectors.
</success_criteria>

<<outputoutput>
After completion, create `.planning/phases/05-projection-infrastructure/05-02-SUMMARY.md`
</output>
