---
phase: 05-projection-infrastructure
plan: 04
type: execute
wave: 4
depends_on: [03]
files_modified:
  - packages/kernel/src/Domain/Shared/Event/Projectors/AggregateSummaryProjector.php
  - packages/kernel/resources/sql/projection/002_create_aggregate_summary.sql
autonomous: true
requirements:
  - PROJ-04
must_haves:
  truths:
    - "Events for a specific aggregate result in a row in aggregate_summary"
    - "The read model provides a flattened view of the aggregate state"
  artifacts:
    - path: "packages/kernel/src/Domain/Shared/Event/Projectors/AggregateSummaryProjector.php"
      provides: "Concrete projector implementation"
    - path: "packages/kernel/resources/sql/projection/002_create_aggregate_summary.sql"
      provides: "Read model table"
  key_links:
    - from: "AggregateSummaryProjector"
      to: "aggregate_summary table"
      via: "UPDATE/INSERT SQL"
---

<<<objectiveobjectiveobjective>
Implement a Proof-of-Concept Read Model.

Purpose: Prove the "Event $\to$ Projector $\to$ Read Model" cycle works end-to-end.
Output: `AggregateSummaryProjector` and `aggregate_summary` read model table.
</objective>

<<<executionexecutionexecution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<<<contextcontextcontext>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/kernel/src/Domain/Shared/Event/AbstractProjector.php
</context>

<<<tasktaskstasks>

<<<tasktasktask type="auto">
  <<<namnamename>Task 1: Create Aggregate Summary Table</name>
  <<<filesfilesfiles>packages/kernel/resources/sql/projection/002_create_aggregate_summary.sql</files>
  <<<actionactionaction>
    Create a simple read-model table `aggregate_summary`.
    Columns:
    - `aggregate_id` (UUID, PK)
    - `tenant_id` (UUID)
    - `last_event_type` (VARCHAR)
    - `last_updated_at` (TIMESTAMP)
    - `version` (INT)
  </action>
  <<<verifyverifyverify>
    <<<automatedautomatedautomated>grep "aggregate_summary" packages/kernel/resources/sql/projection/002_create_aggregate_summary.sql</automated>
  </verify>
  <<<dondonedone>Read model table defined</done>
</task>

<<<tasktasktask type="auto">
  <<<namnamename>Task 2: Implement AggregateSummaryProjector</name>
  <<<filesfilesfiles>packages/kernel/src/Domain/Shared/Event/Projectors/AggregateSummaryProjector.php</files>
  <<<actionactionaction>
    Implement `AggregateSummaryProjector` extending `AbstractProjector`.
    - Subscribe to all domain events (or a representative subset).
    - Implement `handleEvent(DomainEvent $event)`:
      - Perform an UPSERT into the `aggregate_summary` table.
      - Update `last_event_type` and `version` based on the event metadata.
  </action>
  <<<verifyverifyverify>
    <<<automatedautomatedautomated>ls packages/kernel/src/Domain/Shared/Event/Projectors/AggregateSummaryProjector.php</automated>
  </verify>
  <<<dondonedone>Concrete projector implemented</done>
</task>

</tasks>

<<<verificationverificationverification>
End-to-End Integration Test:
1. Append an event to the `PostgreSqlEventStore`.
2. Run `ProjectionEngine::run()`.
3. Query the `aggregate_summary` table.
4. Assert that the row for the aggregate exists and contains the correct `last_event_type` and `version`.
</verification>

<<<successsuccesssuccess_criteria>
The kernel demonstrates a full loop: Write Event $\to$ Poll $\to$ Project $\to$ Read Model.
</success_criteria>

<<<outputoutputoutput>
After completion, create `.planning/phases/05-projection-infrastructure/05-04-SUMMARY.md`
</output>
