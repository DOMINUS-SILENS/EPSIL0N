---
phase: 6-cqrs-routing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/kernel/src/Application/Contract/Query/IQuery.php
  - packages/kernel/src/Application/Contract/Handler/IQueryHandler.php
  - packages/kernel/src/Application/Contract/Bus/IQueryBus.php
  - packages/kernel/src/Application/Bus/QueryBus.php
  - packages/kernel/src/Application/Behavior/Idempotency/IdempotencyGuard.php
autonomous: true
requirements:
  - CQRS-01
  - CQRS-02
  - CQRS-03
user_setup: []

must_haves:
  truths:
    - "Commands dispatch via ICommandBus to ICommandHandler"
    - "Queries dispatch via IQueryBus to IQueryHandler"
    - "Duplicate commands with same CorrelationId are deduplicated"
    - "Command and Query paths are completely separate"
  artifacts:
    - path: "packages/kernel/src/Application/Contract/Query/IQuery.php"
      provides: "Marker interface for query objects"
      contains: "interface IQuery"
    - path: "packages/kernel/src/Application/Contract/Handler/IQueryHandler.php"
      provides: "Handler contract for query objects"
      contains: "interface IQueryHandler"
    - path: "packages/kernel/src/Application/Contract/Bus/IQueryBus.php"
      provides: "Query dispatcher contract"
      contains: "interface IQueryBus"
    - path: "packages/kernel/src/Application/Bus/QueryBus.php"
      provides: "Query dispatch implementation"
      contains: "class QueryBus implements IQueryBus"
    - path: "packages/kernel/src/Application/Behavior/Idempotency/IdempotencyGuard.php"
      provides: "CorrelationId-based command deduplication"
      contains: "class IdempotencyGuard"
  key_links:
    - from: "QueryBus"
      to: "IQueryHandler"
      via: "handler registry"
      pattern: "handlers\\[.*\\]"
    - from: "IdempotencyGuard"
      to: "IIdempotencyService"
      via: "constructor injection"
      pattern: "IIdempotencyService"
    - from: "IdempotencyGuard"
      to: "CorrelationId"
      via: "method parameter"
      pattern: "CorrelationId"
---

<objective>
Implement CQRS routing layer to establish clean separation between Command and Query handling paths. Create Query bus infrastructure parallel to existing Command bus, plus idempotency guard for deduplication.

Purpose: Enable distinct read and write pathways per CQRS pattern - commands mutate state via CommandBus, queries read state via QueryBus.
Output: Five new files (IQuery, IQueryHandler, IQueryBus, QueryBus, IdempotencyGuard)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@packages/kernel/src/Application/Contract/Command/ICommand.php
@packages/kernel/src/Application/Contract/Bus/ICommandBus.php
@packages/kernel/src/Application/Contract/Handler/ICommandHandler.php
@packages/kernel/src/Application/Contract/Idempotency/IIdempotencyService.php
@packages/kernel/src/Application/Bus/CommandBus.php

# Interface patterns extracted from existing code:

ICommand interface (marker):
```php
interface ICommand
{
}
```

ICommandBus interface:
```php
interface ICommandBus
{
    public function dispatch(ICommand $command): Result;
}
```

ICommandHandler interface:
```php
interface ICommandHandler
{
    public function handle(ICommand $command): Result;
}
```

CommandBus pattern (register + dispatch):
```php
final class CommandBus implements ICommandBus
{
    private array $handlers = [];
    public function registerHandler(string $commandClass, ICommandHandler $handler): void;
    public function dispatch(ICommand $command): Result;
}
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create IQuery marker interface</name>
  <files>packages/kernel/src/Application/Contract/Query/IQuery.php</files>
  <action>
Create minimal marker interface for query objects, following ICommand pattern.

- Create file at `src/Application/Contract/Query/IQuery.php`
- Namespace: `Spiral\Kernel\Application\Contract\Query`
- Add `@template TResult` generic for return type annotation
- Use strict_types=1 declaration
  </action>
  <verify>
<automated>ls -la packages/kernel/src/Application/Contract/Query/IQuery.php</automated>
  </verify>
  <done>IQuery interface exists with @template TResult annotation</done>
</task>

<task type="auto">
  <name>Task 2: Create IQueryHandler interface</name>
  <files>packages/kernel/src/Application/Contract/Handler/IQueryHandler.php</files>
  <action>
Create handler contract for query objects, mirroring ICommandHandler structure.

- Create file at `src/Application/Contract/Handler/IQueryHandler.php`
- Namespace: `Spiral\Kernel\Application\Contract\Handler`
- Import IQuery from Query namespace
- Import Result from Domain\Shared namespace
- Define `handle(IQuery $query): Result` method
- Add @template annotations for generic typing (TQuery extends IQuery, TResult)
  </action>
  <verify>
<automated>ls -la packages/kernel/src/Application/Contract/Handler/IQueryHandler.php</automated>
  </verify>
  <done>IQueryHandler interface exists with handle(IQuery): Result method</done>
</task>

<task type="auto">
  <name>Task 3: Create IQueryBus interface</name>
  <files>packages/kernel/src/Application/Contract/Bus/IQueryBus.php</files>
  <action>
Create query dispatcher contract, mirroring ICommandBus pattern.

- Create file at `src/Application/Contract/Bus/IQueryBus.php`
- Namespace: `Spiral\Kernel\Application\Contract\Bus`
- Import IQuery from Query namespace
- Import Result from Domain\Shared namespace
- Define `dispatch(IQuery $query): Result` method
- Add @template TResult for return type annotation
  </action>
  <verify>
<automated>ls -la packages/kernel/src/Application/Contract/Bus/IQueryBus.php</automated>
  </verify>
  <done>IQueryBus interface exists with dispatch method</done>
</task>

<task type="auto">
  <name>Task 4: Create QueryBus implementation</name>
  <files>packages/kernel/src/Application/Bus/QueryBus.php</files>
  <action>
Implement query dispatcher using handler registry pattern.

- Create file at `src/Application/Bus/QueryBus.php`
- Namespace: `Spiral\Kernel\Application\Bus`
- Implement IQueryBus interface
- Add private array $handlers property
- Implement `registerHandler(string $queryClass, IQueryHandler $handler): void`
- Implement `dispatch(IQuery $query): Result` that:
  - Gets query class via get_class()
  - Looks up handler in registry
  - Throws RuntimeException if no handler found
  - Delegates to handler.handle($query)
  - Follow exact same pattern as CommandBus
  </action>
  <verify>
<automated>ls -la packages/kernel/src/Application/Bus/QueryBus.php</automated>
  </verify>
  <done>QueryBus implementation exists with registerHandler and dispatch methods</done>
</task>

<task type="auto">
  <name>Task 5: Create IdempotencyGuard</name>
  <files>packages/kernel/src/Application/Behavior/Idempotency/IdempotencyGuard.php</files>
  <action>
Create command deduplication guard using CorrelationId.

- Create file at `src/Application/Behavior/Idempotency/IdempotencyGuard.php`
- Namespace: `Spiral\Kernel\Application\Behavior\Idempotency`
- Import IIdempotencyService from Contract\Idempotency namespace
- Import CorrelationId from Domain\Identity namespace
- Import Result from Domain\Shared namespace
- Inject IIdempotencyService via constructor
- Create `guard(CorrelationId $correlationId, callable $operation): Result` method that:
  - Calls $idempotencyService->isProcessed($correlationId)
  - If already processed: return cached result (or empty success)
  - If new: execute $operation(), then markAsProcessed()
- This is the application-level guard, not the service itself
  </action>
  <verify>
<automated>ls -la packages/kernel/src/Application/Behavior/Idempotency/IdempotencyGuard.php</automated>
  </verify>
  <done>IdempotencyGuard exists with guard() method using CorrelationId</done>
</task>

</tasks>

<verification>
# Run PHPStan to verify type safety
cd packages/kernel && ./vendor/bin/phpstan analyse src/Application/Contract/Query/IQuery.php src/Application/Contract/Handler/IQueryHandler.php src/Application/Contract/Bus/IQueryBus.php src/Application/Bus/QueryBus.php src/Application/Behavior/Idempotency/IdempotencyGuard.php --level=9
</verification>

<success_criteria>
1. All five files exist with correct namespaces
2. PHPStan level 9 passes with no errors
3. QueryBus follows same pattern as CommandBus
4. IdempotencyGuard uses CorrelationId and IIdempotencyService
5. Command and Query paths are structurally separate
</success_criteria>

<output>
After completion, create `.planning/phases/6-cqrs-routing/06-01-SUMMARY.md`
</output>