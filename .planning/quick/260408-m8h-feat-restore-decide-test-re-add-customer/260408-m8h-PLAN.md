---
phase: quick
task: 260408-m8h
type: execute
autonomous: true
requirements: []
files_modified:
  - packages/kernel/src/Domain/Customer/Customer.php
user_setup: []

must_haves:
  truths:
    - "Customer::decide() is restored as @internal test helper"
    - "Production and test APIs are clearly separated"
    - "All test events (CustomerCreated, CustomerRenamed) have handlers"
    - "Tests pass with new changes"
  artifacts:
    - path: "packages/kernel/src/Domain/Customer/Customer.php"
      provides: "Customer aggregate with decide() test method"
      contains:
        - "@internal docblock on decide()"
        - "handleCreateCustomer() and handleRenameCustomer() private helpers"
        - "applyCreated() and applySimpleRenamed() event handlers"
  key_links:
    - from: "Customer::decide()"
      to: "handleCreateCustomer() / handleRenameCustomer()"
      via: "dispatch in match expression"
    - from: "handleCreateCustomer() / handleRenameCustomer()"
      to: "applyCreated() / applySimpleRenamed()"
      via: "event application in apply()"
---

<objective>
Restore Customer::decide() as an @internal test-only method with clear separation from production event-sourced API (register(), rename(), etc.).

Purpose: Enable testing of aggregate behavior at domain level while maintaining production flow distinction.

Output: Committed Customer.php with decide() restoration and supporting test helpers.
</objective>

<execution_context>
@/home/dominus/Project/EPSILON/CLAUDE.md
@/home/dominus/Project/EPSILON/.planning/STATE.md
</execution_context>

<context>
Changes prepared in working directory:
- Added decide() with @internal marker and docblock
- Added handleCreateCustomer() and handleRenameCustomer() private helpers
- Added applyCreated() and applySimpleRenamed() event handlers for test events
- Added import statements for CustomerCreated and CustomerRenamed test event classes

The diff maintains the distinction between:
- Production: register(), rename(), verifyEmail(), deactivate(), reactivate() (event-sourced)
- Testing: decide() (command-handler pattern, @internal only)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and commit Customer::decide() restoration</name>
  <files>packages/kernel/src/Domain/Customer/Customer.php</files>
  <action>
    1. Verify the diff is correct:
       - decide() method has @internal marker and clear docblock
       - handleCreateCustomer() and handleRenameCustomer() private methods exist
       - applyCreated() and applySimpleRenamed() event handlers implemented
       - Import statements for CustomerCreated and CustomerRenamed classes added

    2. Run static analysis to ensure code quality:
       cd /home/dominus/Project/EPSILON/packages/kernel
       ./vendor/bin/phpstan analyse src/Domain/Customer/Customer.php --level=9

    3. Run Customer tests to verify no regressions:
       cd /home/dominus/Project/EPSILON/packages/kernel
       ./vendor/bin/phpunit tests/Unit/Domain/Customer/ -v

    4. Commit the changes with message:
       git add packages/kernel/src/Domain/Customer/Customer.php
       git commit -m "feat(260408-m8h): restore Customer::decide() as @internal test helper

       - Re-add decide() method with @internal marker and clear docblock
       - Separate production event-sourced API (register, rename, etc) from test API (decide)
       - Add handleCreateCustomer() and handleRenameCustomer() private command handlers
       - Add applyCreated() and applySimpleRenamed() event handlers for test events
       - Maintain LSP compliance: no dual-execution paths, clear method contracts

       Per prior decision: decide() is testing helper only, not production interface."
  </action>
  <verify>
    <automated>cd /home/dominus/Project/EPSILON/packages/kernel && ./vendor/bin/phpunit tests/Unit/Domain/Customer/ -v</automated>
  </verify>
  <done>Customer.php committed with decide() restoration, static analysis passes, unit tests pass, git status clean</done>
</task>

</tasks>

<verification>
- PHP static analysis (PHPStan level 9) passes with no errors
- Unit tests for Customer aggregate pass
- Git commit created with proper message
- Working tree clean
</verification>

<success_criteria>
- Commit created with hash in log
- Customer::decide() method visible with @internal marker
- All test event handlers properly implemented
- No regressions in existing tests
- Code follows project conventions (strict types, immutability, clear separation)
</success_criteria>

<output>
After completion, update `.planning/STATE.md` quick tasks section:
- Add entry: `260408-m8h | Restore Customer::decide() as @internal test helper | 2026-04-08 | {commit-hash} | 260408-m8h-feat-restore-decide-test-re-add-customer`
</output>
