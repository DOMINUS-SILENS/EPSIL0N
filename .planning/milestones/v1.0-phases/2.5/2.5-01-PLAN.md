---
phase: 2.5-runtime-spine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/kernel/src/Infrastructure/Contract/Persistence/IRepository.php
  - packages/kernel/src/Infrastructure/Persistence/EventSourcedRepository.php
  - packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php
  - packages/kernel/resources/sql/event_store/001_create_event_store.sql
autonomous: true
requirements:
  - PG-01
  - PG-02
  - PG-03
  - PG-04
  - PG-05
  - TEN-01
  - TEN-02
  - TEN-03
  - TEN-04
user_setup: []

must_haves:
  truths:
    - "Aggregates can be rehydrated from the event store"
    - "Concurrent writes to the same stream version are rejected by PostgreSQL"
    - "Cross-tenant access is blocked at the database query level"
    - "Optimistic concurrency failures throw ConcurrencyConflictException"
  artifacts:
    - path: "packages/kernel/src/Infrastructure/Contract/Persistence/IRepository.php"
      provides: "Generic repository contract"
    - path: "packages/kernel/src/Infrastructure/Persistence/EventSourcedRepository.php"
      provides: "Generic ES implementation for AggregateRoot lifecycle"
    - path: "packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php"
      provides: "Hardened PG event store with DB-level concurrency checks"
  key_links:
    - from: "EventSourcedRepository"
      to: "PostgreSqlEventStore"
      via: "IEventStore interface"
      pattern: "append\\(\\$tenantId, \\$streamId, \\$expectedVersion, \\$events\\)"
    - from: "PostgreSqlEventStore"
      to: "PostgreSQL"
      via: "UNIQUE constraint on (stream_id, stream_version)"
      pattern: "UNIQUE \\(stream_id, stream_version\\)"
---

<objective>
Transform the EPSILON Kernel from a semantic substrate to an operational execution engine by implementing the Runtime Spine.

Purpose: Establish the "Load $\rightarrow$ Mutate $\rightarrow$ Save" cycle and harden optimistic concurrency at the database level.
Output: A functional EventSourcedRepository and a hardened PostgreSqlEventStore.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/2.5/2.5-RESEARCH.md
@packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden Database Schema for Concurrency</name>
  <files>packages/kernel/resources/sql/event_store/001_create_event_store.sql</files>
  <action>
    Modify the event store table definition to include a UNIQUE constraint on `(stream_id, stream_version)`. 
    This ensures that the database is the final arbiter of optimistic concurrency, preventing race conditions that PHP-level checks cannot catch.
  </action>
  <verify>
    <automated># Check if the SQL file contains the UNIQUE constraint</automated>
  </verify>
  <done>SQL schema updated with UNIQUE(stream_id, stream_version)</done>
</task>

<task type="auto">
  <name>Task 2: Implement IRepository Contract</name>
  <files>packages/kernel/src/Infrastructure/Contract/Persistence/IRepository.php</files>
  <action>
    Create the `IRepository` interface using generics. 
    It must define `getById(TId $id, TenantId $tenantId): T` and `save(T $aggregate): void`.
    Ensure `TenantId` is a mandatory parameter for retrieval to enforce structural isolation.
  </action>
  <verify>
    <automated>./vendor/bin/phpstan analyse packages/kernel/src/Infrastructure/Contract/Persistence/IRepository.php</automated>
  </verify>
  <done>IRepository interface created and type-safe</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement EventSourcedRepository</name>
  <files>
    packages/kernel/src/Infrastructure/Persistence/EventSourcedRepository.php,
    packages/kernel/tests/Integration/Persistence/EventSourcedRepositoryTest.php
  </files>
  <behavior>
    - Test 1: Loading an aggregate re-applies events in order and restores state.
    - Test 2: Saving an aggregate appends only uncommitted events.
    - Test 3: Saving an aggregate updates its internal version upon success.
    - Test 4: Rejects loading an aggregate if the TenantId does not match the stream.
  </behavior>
  <action>
    Implement `EventSourcedRepository` which coordinates between `AggregateRoot` and `IEventStore`.
    Logic:
    - `getById`: Load events from store $\rightarrow$ instantiate aggregate $\rightarrow$ reconstitute.
    - `save`: Extract uncommitted events $\rightarrow$ append to store using current aggregate version as `ExpectedVersion` $\rightarrow$ mark committed.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit packages/kernel/tests/Integration/Persistence/EventSourcedRepositoryTest.php</automated>
  </verify>
  <done>EventSourcedRepository correctly manages AggregateRoot lifecycle</done>
</task>

<task type="auto">
  <name>Task 4: Hardened PostgreSqlEventStore Implementation</name>
  <files>packages/kernel/src/Infrastructure/Persistence/EventStore/PostgreSqlEventStore.php</files>
  <action>
    Refactor `PostgreSqlEventStore::append` to rely on the database UNIQUE constraint for concurrency.
    1. Remove the PHP-level `validateExpectedVersion` call.
    2. Wrap the INSERT in a transaction.
    3. Catch `PDOException` with SQLSTATE `23505` (unique violation).
    4. Map the violation to `ConcurrencyConflictException` with accurate version details.
    5. Maintain the `TenantId` check in all queries to prevent cross-tenant leakage.
  </action>
  <verify>
    <automated>./vendor/bin/phpunit packages/kernel/tests/Integration/EventStore/EventStoreTest::testRejectsStaleVersion</automated>
  </verify>
  <done>Concurrency enforcement moved to DB layer and mapped to Domain exception</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Runtime Spine (Repository + Hardened Event Store)</what-built>
  <how-to-verify>
    Run the full integration suite:
    `cd packages/kernel && ./vendor/bin/phpunit --testsuite Integration`
    Verify specifically that:
    1. Aggregates are persisted and reloaded perfectly.
    2. A concurrent save attempt (simulated via two repository instances) triggers a `ConcurrencyConflictException`.
    3. Attempting to load an aggregate using a different `TenantId` than the one it was saved with returns a 404 or throws an isolation exception.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
The phase is complete when the "Load $\rightarrow$ Mutate $\rightarrow$ Save" cycle is proven stable and concurrency is enforced by the database.
</verification>

<success_criteria>
- [ ] `EventSourcedRepository` successfully rehydrates aggregates.
- [ ] `PostgreSqlEventStore` throws `ConcurrencyConflictException` on SQL unique violation.
- [ ] All integration tests in `tests/Integration/EventStore` and `tests/Integration/Persistence` pass.
- [ ] `TenantId` is verified on every load and save operation.
</success_criteria>

<output>
After completion, create `.planning/phases/2.5-runtime-spine/2.5-01-SUMMARY.md`
</output>
